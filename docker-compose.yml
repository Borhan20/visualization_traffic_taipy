version: "3.10"

services:
  taipy-app:
    build: .
    container_name: taipy_app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
